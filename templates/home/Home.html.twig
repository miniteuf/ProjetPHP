{% extends "base.html.twig" %}
{% block body %}

    
{# <div name="slider-games">
    <div class="row">
        {% for games in listGames %}
            <div class="col-md-4">
                <div class="card box-shadow">
                <div class="card-body">
                    <h2 class="card-text font-weight-bold">{{ games.Name }}</h2>
                    <p class="card-text">{{ games.description }}</p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group">
                            <a href='{{ path('gamesView', {'id': games.getIdentifier}) }}' class="btn btn-info">Voir le jeu</a>
                        </div>
                    </div>
                </div>   
                </div>
            </div>
        {% endfor %}
    </div>
</div>
<div>
<hr / >
</div> #}
<h1>Accueil</h1>
<hr />
{# <div name="slider-category_game">
    <div class="row">
        {% for categoryGames in listCategoryGames %}
            <div class="col-md-4">
                <div class="card box-shadow">
                    <div class="card-body">
                        <h2 class="card-text font-weight-bold">{{ categoryGames.name }}</h2>
                        <h2 class="card-text font-weight-bold">{{ categoryGames.description }}</h2>
                    </div>
                </div>   
            </div>
        {% endfor %}
    </div>
</div> #}
{% endblock %}